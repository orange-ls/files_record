--- 未完成 ---

```js
hWeb = WebBrowser.BindBrowser("chrome",10000,{"bContinueOnError":False,"iDelayAfter":300,"iDelayBefore":200})
fieldList = []
fieldStr = Join(fieldList,";")
//点击【字段导出设置按钮】
#icon("@res:vi20sp9t-n83g-mg1m-ld4g-mgdpkkv4otie.png")
Mouse.Action({"wnd":[{"cls":"Chrome_WidgetWin_1","title":"*","app":"chrome"},{"cls":"Chrome_RenderWidgetHostHWND","title":"Chrome Legacy Window"}],"html":[{"tag":"IFRAME","src":"https://partner.huawei.com/web/visi/#/cn1*"},{"tag":"svg","css-selector":"body>div>div>div>div>div>div>div>div>div>div>div>div>div>div>svg"}]},"left","click",10000,{"bContinueOnError":False,"iDelayAfter":1000,"iDelayBefore":200,"bSetForeground":True,"sCursorPosition":"Center","iCursorOffsetX":0,"iCursorOffsetY":0,"sKeyModifiers":[],"sSimulate":"uia","bMoveSmoothly":False})
//设置字段（具体js可见路径：extend\python\华为订单表下载字段设置js.txt）
sRet = WebBrowser.RunJS(hWeb,
'''
function () {
    /* -------------------------------------------------- 基础准备 -------------------------------------------------- */
    // 当前操作的 iframe（通过 xpath 定位）
    const xpathIframe = "//div[contains(concat(' ', normalize-space(@class), ' '), 'aui-grid-custom__tabs-body')]";
    const iframeEle = document.evaluate(
        xpathIframe,
        document,
        null,
        XPathResult.FIRST_ORDERED_NODE_TYPE,
        null
    ).singleNodeValue;

    if (!iframeEle) {
        console.error('未找到目标 iframe：', xpathIframe);
        return null;
    }

    /* -------------------------------------------------- 1. 展开所有父节点 ----------------------------------------- */
    const mainEles = iframeEle.querySelectorAll("div[class='aui-grid-body__treenode']");
    for (let i = 0; i < mainEles.length; i++) {
        mainEles[i].click();          // 点击展开
    }

    /* -------------------------------------------------- 2. 等待子节点渲染 ----------------------------------------- */
    setTimeout(function () {
        console.log('等待 3s，否则子字段还未加载');

        // 构造 click 事件（svg 元素不支持原生 .click()）
        const event = new MouseEvent('click');

        // 所有“复选框”svg 元素
        const eles = iframeEle.querySelectorAll(
            'div.aui-grid-cell > div > span:nth-child(1) > svg'
        );

        // 需要勾选的字段列表（外部通过 '''&fieldStr&''' 注入，用分号分隔）
        const checkList = "'''&fieldStr&'''".split(';');
        console.log('待勾选字段列表：', checkList);
        console.log('共找到 svg 复选框数量：', eles.length);

        /* ------------------------------------------------ 3. 遍历并反选/取消 --------------------------------------- */
        for (let i = 0; i < eles.length; i++) {
            // 取字段名（向上找 5 层再取前一个兄弟节点的文本）
            const eleText = eles[i]
                .parentElement.parentElement.parentElement
                .parentElement.parentElement.previousElementSibling
                .innerText.trim();

            const isInCheckList = checkList.indexOf(eleText) !== -1;
            const cls = eles[i].getAttribute('class');

            if (!isInCheckList) {
                // 不在列表中 -> 需要“未勾选”
                if (cls.indexOf('hide') !== -1) {
                    // 当前已勾选 -> 取消勾选
                    eles[i].dispatchEvent(event);
                }
            } else {
                // 在列表中 -> 需要“勾选”
                if (cls.indexOf('open') !== -1) {
                    // 当前未勾选 -> 勾选
                    eles[i].dispatchEvent(event);
                }
            }
        }

        return 1;   // 异步完成标志
    }, 3000);

    return 2;}     // 同步返回标志
''',True,{"bContinueOnError":False,"iDelayAfter":3000,"iDelayBefore":200})
```





```js
function(){/*当前操作的iframe*/const xpathIframe = "//div[contains(concat(' ', normalize-space(@class), ' '),  'aui-grid-custom__tabs-body')]";const iframeEle = document.evaluate(xpathIframe, document, null, XPathResult.FIRST_ORDERED_NODE_TYPE, null).singleNodeValue;if (!iframeEle) {console.error("未找到目标 iframe：", xpathIframe); return null; }/*所有字段父字段元素*/var mainEles = iframeEle.querySelectorAll("div[class='aui-grid-body__treenode']");/*展开所有字段*/for(i=0; i < mainEles.length; i++){    mainEles[i].click();}setTimeout(function() {        console.log("等待3s，否则子字段还未加载");        /* 定义click事件的event（svg无法使用.click，需要调用dispatchEvent）*/        var event = new MouseEvent('click');        /* 获取所有子字段元素*/        var eles = iframeEle.querySelectorAll("div.aui-grid-cell > div > span:nth-child(1) > svg");        /*需要勾选的字段列表*/        var checkList = "'''&fieldStr&'''".split(";");        console.log(checkList);        console.log(eles.length);        for(i=0; i<eles.length; i++){                /*判断字段是否是要勾选的*/                eleText = eles[i].parentElement.parentElement.parentElement.parentElement.parentElement.previousElementSibling.innerText.trim();                if(checkList.indexOf(eleText) == -1){                        /*判断该字段是否已勾选*/                        if(eles[i].getAttribute("class").indexOf("hide") != -1){                                eles[i].dispatchEvent(event);                        }                }else{                        /*字段无需勾选，判断该字段是否需要取消勾选*/                        if(eles[i].getAttribute("class").indexOf("open") != -1){                                eles[i].dispatchEvent(event);                        }                }        }        return 1;}, 3000);return 2;}
```



旧：

```js
function(){/*当前操作的iframe*/var iframeEle = document.querySelector("iframe.epartner-iframe").contentWindow.document;/*所有字段父字段元素*/var mainEles=iframeEle.querySelectorAll("span[class='aui-grid-tree-wrapper']");/*展开所有字段*/for(i=0; i < mainEles.length; i++){    mainEles[i].click();}setTimeout(function() {        console.log("等待3s，否则子字段还未加载");        /* 定义click事件的event（svg无法使用.click，需要调用dispatchEvent）*/        var event = new MouseEvent('click');        /* 获取所有子字段元素*/        var eles = iframeEle.querySelectorAll("div.aui-grid-cell > div > span:nth-child(1) > svg");        /*需要勾选的字段列表*/        var checkList = "'''&fieldStr&'''".split(";");        console.log(checkList);        console.log(eles.length);        for(i=0; i<eles.length; i++){                /*判断字段是否是要勾选的*/                eleText = eles[i].parentElement.parentElement.parentElement.parentElement.parentElement.previousElementSibling.innerText.trim();                if(checkList.indexOf(eleText) == -1){                        /*判断该字段是否已勾选*/                        if(eles[i].getAttribute("class").indexOf("hide") != -1){                                eles[i].dispatchEvent(event);                        }                }else{                        /*字段无需勾选，判断该字段是否需要取消勾选*/                        if(eles[i].getAttribute("class").indexOf("open") != -1){                                eles[i].dispatchEvent(event);                        }                }        }        return 1;}, 3000);return 2;}
```





旧修改：

```js
a = function(fieldStr){/*当前操作的iframe*/var iframeEle = document.querySelector("iframe.epartner-iframe").contentWindow.document;/*所有字段父字段元素*/var mainEles=iframeEle.querySelectorAll("div[class='aui-grid-body__treenode']");/*展开所有字段*/for(i=0; i < mainEles.length; i++){    mainEles[i].click();}setTimeout(function() {        console.log("等待3s，否则子字段还未加载");        /* 定义click事件的event（svg无法使用.click，需要调用dispatchEvent）*/        var event = new MouseEvent('click');        /* 获取所有子字段元素*/        var eles = iframeEle.querySelectorAll("div.aui-grid-cell > div > span:nth-child(1) > svg");        /*需要勾选的字段列表*/        var checkList = fieldStr.split(";");        console.log(checkList);        console.log(eles.length);        for(i=0; i<eles.length; i++){                /*判断字段是否是要勾选的*/                eleText = eles[i].parentElement.parentElement.parentElement.parentElement.parentElement.previousElementSibling.innerText.trim();                if(checkList.indexOf(eleText) == -1){                        /*判断该字段是否已勾选*/                        if(eles[i].getAttribute("class").indexOf("hide") != -1){                                eles[i].dispatchEvent(event);                        }                }else{                        /*字段无需勾选，判断该字段是否需要取消勾选*/                        if(eles[i].getAttribute("class").indexOf("open") != -1){                                eles[i].dispatchEvent(event);                        }                }        }        return 1;}, 3000);return 2;}
```

